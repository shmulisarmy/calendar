# calendar

this is a webiste where users can have a calendar to wich they can add events to it


potential features 

- show schedule compatibility with other users

run command (localhost)
```sh
uvicorn main:app --reload
```

go to localhost:8000/docs

